{
  "scripts": {
    "test": "truffle test --debug --network development",
    "ganache": "ganache-cli --accounts 50>/dev/null"
  },
  "dependencies": {
    "@chainlink/contracts": "^0.1.7",
    "@openzeppelin/contracts": "^3.4.1",
    "concurrently": "^6.0.2",
    "ganache-cli": "^6.12.2",
    "truffle-assertions": "^0.9.2"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
